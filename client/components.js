var Counter = require('../universal/Counter')
var Incrementer = require('../universal/Incrementer')

/**
 * Universal components must be registered here in order to enable
 * client side rendering / mounting
 */
module.exports = {
  Counter,
  Incrementer
}
